#include<stdio.h> 

int main() 
{ 
    int r=0,sum=0,n; 
    printf("\nEnter the no:"); 
    scanf("%d",&n); 
    while(n>0) 
    { 
        r=n%10; 
        sum=sum*10+r; 
        n=n/10; 
    } 
    printf("Reverse of the number is:%d",sum); 
    return 0; 
} 
